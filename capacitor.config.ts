import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.gestioncolectivos',
  appName: 'gestioncolectivos',
  webDir: 'www'
};

export default config;
